name: CI Pipeline for Backend and Frontend

on:
  push:
    branches: [ "main" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      # --- Backend (Java/Maven) Steps ---
      - name: Set up JDK 21
        uses: actions/setup-java@v4
        with:
          java-version: '21'
          distribution: 'temurin'
          cache: 'maven'

      - name: Cache SonarCloud packages
        uses: actions/cache@v4
        with:
          path: ~/.sonar/cache
          key: ${{ runner.os }}-sonar
          restore-keys: ${{ runner.os }}-sonar

      - name: Build, Test and Analyze Backend
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
        run: mvn -B verify org.sonarsource.scanner.maven:sonar-maven-plugin:sonar

      # --- Frontend (Angular) Steps ---
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: frontend/package-lock.json

      - name: Install Frontend Dependencies
        run: cd frontend && npm ci

      - name: Run Dependency Audit
        run: cd frontend && npm audit --audit-level=high --registry=https://registry.npmjs.org

      - name: Run Frontend Unit Tests
        run: cd frontend && ./node_modules/.bin/ng test --watch=false --browsers=ChromeHeadless --code-coverage

      - name: Install Playwright Browsers
        run: cd frontend && ./node_modules/.bin/playwright install --with-deps

      - name: Run Frontend E2E Tests
        run: cd frontend && ./node_modules/.bin/playwright test

      - name: Build Frontend for ZAP Scan
        run: cd frontend && ./node_modules/.bin/ng build

      - name: Run ZAP Baseline Scan
        uses: zaproxy/action-baseline@v0.11.0
        with:
          target: 'file://${{ github.workspace }}/frontend/dist/frontend/browser/index.html'
          fail_action: true
          # High-level alerts will fail the build
          min_level: 'high'

      # --- SonarCloud Analysis for Frontend ---
      - name: Run SonarCloud Scan for Frontend
        uses: SonarSource/sonarcloud-github-action@master
        with:
          projectBaseDir: frontend
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          SONAR_TOKEN: ${{ secrets.SONAR_TOKEN }}
